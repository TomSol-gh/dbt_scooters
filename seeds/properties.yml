version: 2

seeds:
  - name: "models"
    description: "Scooter models info and statistics"
    config:
      delimiter: ","
  - name: "event_types"
    description: "User events in mobile app"
    config:
      delimiter: ","
  - name: "age_groups"
    description: "User age groups"
    config:
      delimiter: ","